# TaskHub QA Sandbox

Учебный стенд для автоматизатора тестирования с полным стеком технологий: Frontend (HTML/JS) + Backend (Node.js/Express) + БД (SQLite) + REST API + Swagger + JWT Authentication.

## 🚀 Демо

**Посмотреть проект:** https://github.com/Mars4325/Create-project

## 🔐 Быстрый старт

## 🔐 Аутентификация

Проект включает полную систему аутентификации с JWT токенами и отдельными страницами входа.

### 🚪 Страницы аутентификации

- **Главная страница:** `http://localhost:3000/index.html`
- **Вход:** `http://localhost:3000/login.html`
- **Регистрация:** `http://localhost:3000/register.html`

### 🎯 Как использовать

1. **Первый заход:** Откройте главную страницу - увидите кнопки "Войти" и "Регистрация"
2. **Регистрация:** Нажмите "Регистрация" → заполните форму → автоматически войдете в систему
3. **Вход:** Нажмите "Войти" → введите логин/пароль → попадете в приложение
4. **Выход:** Нажмите "Выйти" в правом верхнем углу

### 👥 Тестовые аккаунты

**Admin аккаунт:**
- Username: `admin`
- Password: `admin123`
- Роль: Administrator

**System аккаунт:**
- Username: `system`
- Password: `system`

## 🚀 Запуск сервера

### Способ 1: Автоматический (рекомендуется)

1. Найдите файл `START-SERVER.bat` в корне проекта
2. Дважды кликните по нему левой кнопкой мыши
3. Сервер автоматически:
   - Установит зависимости
   - Создаст базу данных
   - Запустится на http://localhost:3000

### Способ 2: Проверка статуса сервера

1. Откройте `server-status.html` в браузере
2. Страница автоматически проверит, работает ли сервер
3. Если сервер не работает - следуйте инструкциям на странице

### Способ 2: Ручной запуск

```bash
cd backend
npm install          # Установка зависимостей
npm run init-db      # Создание базы данных
npm start           # Запуск сервера
```

### Способ 3: Быстрый запуск через cmd

```cmd
cd /d "d:\для курсора\taskhub-qa-sandbox\backend"
npm start
```

### Способ 4: Запуск с автоматическим открытием браузера

```cmd
# Запускает сервер и автоматически открывает браузер
start-and-open.bat
```

## 💾 Сохранение в Git

### ✅ Commit уже создан!

Проект был успешно сохранен в локальном Git репозитории:
- **Commit:** `TaskHub QA Sandbox - Complete Implementation`
- **Изменено файлов:** 55
- **Добавлено строк:** 3374
- **Удалено строк:** 89

### Отправка на GitHub

Если push не удался из-за проблемы с ветками:

#### Способ 1: Автоматическое исправление
```cmd
# Запустите файл для исправления проблемы с ветками
push-fix-manual.bat
```

#### Способ 2: Ручное исправление
```cmd
cd "d:\для курсора\taskhub-qa-sandbox"

# Переименовать ветку master в main
git branch -m master main

# Отправить на GitHub
git push -u origin main
```

#### Способ 3: Инструкции в файле
```
# Откройте GIT-PUSH-FIX.txt для подробных инструкций
```

### Что сохранено в Git

- ✅ **Полный backend:** Express.js, SQLite, JWT, REST API
- ✅ **Frontend:** HTML, CSS, JavaScript с аутентификацией
- ✅ **Безопасность:** CSP, Helmet, bcrypt, CORS
- ✅ **Документация:** README, API docs, инструкции
- ✅ **Тесты:** Jest, Playwright, автоматизированные скрипты
- ✅ **Скрипты:** Запуск, тестирование, развертывание

## ⚠️ Важно: Не открывайте HTML файлы напрямую!

**Ошибка CORS/file:// означает, что вы открыли HTML файл напрямую в браузере.**

**Всегда запускайте сервер и открывайте через http://localhost:3000**

### Правильно ✅:
- Запустить `START-SERVER.bat`
- Открыть `http://localhost:3000`

### Неправильно ❌:
- Двойной клик по `login.html`
- Ввод пути к файлу в браузере
- Протокол `file://`

## 🛡️ Безопасность (CSP)

Проект использует **Content Security Policy (CSP)** для защиты от XSS атак:

### Настройки CSP:
```
defaultSrc: 'self'
scriptSrc: 'self'
styleSrc: 'self', 'unsafe-inline'
connectSrc: 'self'
```

### Безопасность скриптов:
- ✅ Все встроенные скрипты перемещены во внешние файлы
- ✅ Запрещены inline-скрипты для защиты от XSS
- ✅ Разрешены только скрипты из того же домена
- ✅ API работает через HTTPS-подобные заголовки

### Тестирование безопасности:
```bash
# Запуск сервера (вариант 1 - автоматический)
START-SERVER.bat

# Запуск сервера (вариант 2 - ручной)
cd backend
npm install
npm run init-db
npm start

# Проверка CSP: http://localhost:3000/test-csp.html
# Проверка аутентификации: http://localhost:3000/test-new-auth.html
```
- Роль: Administrator

### Первый запуск

1. Запустите сервер: `npm start`
2. Откройте http://localhost:3000
3. Войдите с тестовым аккаунтом или зарегистрируйте нового пользователя

## 🚀 Быстрый старт

### Предварительные требования
- Node.js (версия 16 или выше)
- npm или yarn

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd taskhub-qa-sandbox
   ```

2. **Установите зависимости backend:**
   ```bash
   cd backend
   npm install
   ```

3. **Инициализируйте базу данных:**
   ```bash
   npm run init-db
   ```

4. **Запустите сервер:**
   ```bash
   npm run dev  # для разработки с nodemon
   # или
   npm start    # для production
   ```

5. **Откройте браузер:**
   - **Полное приложение:** http://localhost:3000
   - **Swagger API документация:** http://localhost:3000/api-docs
   - **Health check:** http://localhost:3000/health

## 📋 API Endpoints

### Пользователи (/api/users)
- `GET /api/users` - Получить всех пользователей
- `GET /api/users/:id` - Получить пользователя по ID
- `POST /api/users` - Создать нового пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Проекты (/api/projects)
- `GET /api/projects` - Получить все проекты
- `GET /api/projects/:id` - Получить проект по ID
- `GET /api/projects/owner/:ownerId` - Получить проекты пользователя
- `POST /api/projects` - Создать новый проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект

### Тест-кейсы (/api/test-cases)
- `GET /api/test-cases` - Получить все тест-кейсы (с фильтрами)
- `GET /api/test-cases/:id` - Получить тест-кейс по ID
- `GET /api/test-cases/project/:projectId` - Получить тест-кейсы проекта
- `GET /api/test-cases/assigned/:userId` - Получить назначенные тест-кейсы
- `GET /api/test-cases/project/:projectId/stats` - Статистика тест-кейсов
- `POST /api/test-cases` - Создать новый тест-кейс
- `PUT /api/test-cases/:id` - Обновить тест-кейс
- `DELETE /api/test-cases/:id` - Удалить тест-кейс

## 🗄️ Модель данных

### Пользователь (User)
```json
{
  "id": "uuid",
  "username": "string",
  "email": "string",
  "role": "user|admin|qa",
  "created_at": "datetime",
  "updated_at": "datetime"
}
```

### Проект (Project)
```json
{
  "id": "uuid",
  "name": "string",
  "description": "string",
  "owner_id": "uuid",
  "status": "active|archived|completed",
  "test_cases_count": "number"
}
```

### Тест-кейс (TestCase)
```json
{
  "id": "uuid",
  "title": "string",
  "description": "string",
  "project_id": "uuid",
  "priority": "low|medium|high|critical",
  "status": "draft|review|approved|in_progress|passed|failed|blocked",
  "steps": [
    {
      "step": "string",
      "expected": "string"
    }
  ],
  "expected_result": "string",
  "assigned_to": "uuid|null"
}
```

## 🧪 Структура тестирования

```
tests/
├── api/           # API тесты (Jest + Supertest)
├── ui/            # UI тесты (Playwright)
└── db/            # Тесты базы данных
```

### Запуск тестов
```bash
# API тесты
npm run test:api

# База данных тесты
npm run test:db

# UI тесты
npm run test:ui

# UI тесты в браузере (headed mode)
npm run test:ui:headed

# Все тесты
npm run test:all

# Линтинг кода
npm run lint
```

## 🔧 Конфигурация

### Переменные окружения
Создайте файл `.env` в директории backend:

```env
PORT=3000
NODE_ENV=development
DATABASE_PATH=./database/taskhub.db
```

## 🏗️ Архитектура

```
taskhub-qa-sandbox/
├── backend/              # Node.js/Express сервер
│   ├── src/
│   │   ├── controllers/  # Обработчики запросов
│   │   ├── models/       # Модели данных
│   │   ├── routes/       # Маршруты API
│   │   ├── middleware/   # Промежуточное ПО
│   │   └── database/     # Работа с БД
│   └── package.json
├── frontend/             # HTML/JS клиент
├── tests/               # Автотесты
└── README.md
```

## 🚀 CI/CD

Проект настроен для интеграции с GitHub Actions:

- Автоматические тесты при пуше
- Проверка кода (ESLint)
- Деплой в staging/production

## 📚 Документация

- [Swagger API Docs](http://localhost:3000/api-docs) - интерактивная документация API
- [Postman коллекция](./docs/postman_collection.json) - коллекция запросов для тестирования

## 🤝 Вклад в проект

1. Fork проект
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- **Ваше имя** - *AQA Engineer* - [GitHub](https://github.com/yourusername)

## 🙏 Благодарности

- Express.js за отличный веб-фреймворк
- SQLite за легкую встроенную БД
- Swagger за документацию API
- Playwright за UI тестирование
